import Head from 'next/head'
import Image from 'next/image'
import Map from '../components/Map'
import Sidebar from '../components/Sidebar/Sidebar'
import styles from '../styles/Home.module.css'
import Button from '../components/Button/';
import InfoGraph from '../components/Infograph/';
import SelectInput from '../components/SelectInput'
import styled from "styled-components";
import Input from '../components/Input'
import { Spacer } from '../components/Spacer/Spacer'

const InfoGraphDivs = styled.div`
display:flex;
flex-direction:column;
gap:20px;
`
export default function Home() {
  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
      </Head>
      <div className='flex flex-row justify-between'>
        <Button header={"See Map"} onClick={() => { }} />
        <InfoGraphDivs>
          <InfoGraph header={"$1.5 MILLION"} subhead={"AVERAGE HOME COST"} icon={"/icons/bi_house-fill.svg"} />
          <InfoGraph header={"$2.2K/MONTH"} subhead={"AVERAGE RENT COST"} icon={"/icons/ic_round-apartment.svg"} />
          <SelectInput values={[1, 2, 3]} onChange={(e) => { console.log(e.target.value) }} />
          <Input value={0} type={"number"} onChange={(e)=>{console.log(e.target.value)}}/>
        </InfoGraphDivs>
        {/* <Sidebar /> */}
        {/* <Map /> */}
      </div>
    </>
  )
}
